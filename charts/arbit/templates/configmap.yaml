apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "arbit.fullname" . }}
  labels:
    {{- include "arbit.labels" . | nindent 4 }}
data:
  gateway.yml: |
    {{- .Values.config.gateway | nindent 4 }}
