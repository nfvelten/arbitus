apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "arbitus.fullname" . }}
  labels:
    {{- include "arbitus.labels" . | nindent 4 }}
data:
  gateway.yml: |
    {{- .Values.config.gateway | nindent 4 }}
